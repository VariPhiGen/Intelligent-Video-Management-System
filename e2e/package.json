{
  "name": "vms-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end journeys against a real VMS stack.",
  "scripts": {
    "up": "./scripts/up.sh",
    "down": "./scripts/down.sh",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
